import React from 'react';

const comments=(props)=>{
    /*render(){
        return(
            <div className="posts">
                {comments.map(comment =>(
                    <div id={comment.id} key={comment.id} className="post">
                        <h4 className="title">{comment.name}</h4>
                        <p className="body">{comment.body}</p>
                    </div>
                ))}
            </div>
        );
    }*/
}

export default comments;